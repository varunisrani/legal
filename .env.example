# Environment Variables for Claude Legal Agent API

# Port configuration
PORT=8000

# Claude OAuth Authentication (Required for production)
CLAUDE_CODE_USE_OAUTH=true

# Environment type
ENVIRONMENT=production

# CORS Settings (configure for production)
ALLOWED_ORIGINS=*

# Optional: If you want to use API key instead of OAuth (not recommended)
# ANTHROPIC_API_KEY=your_api_key_here