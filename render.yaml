services:
  - type: web
    name: claude-legal-agent-ts
    env: node
    buildCommand: "npm install && npm run build"
    startCommand: "npm start"
    envVars:
      - key: PORT
        value: "10000"
      - key: NODE_ENV
        value: "production"