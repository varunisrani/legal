services:
  - type: web
    name: claude-legal-agent
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python legal-agent-api-final.py"
    envVars:
      - key: PORT
        value: "10000"